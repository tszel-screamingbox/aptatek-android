<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@color/applicationSolidGray" />

    <size
        android:width="@dimen/pin.circle.size"
        android:height="@dimen/pin.circle.size" />
    <stroke
        android:width="@dimen/general.distance.xxxsmall"
        android:color="@color/applicationSmoke" />
</shape>

