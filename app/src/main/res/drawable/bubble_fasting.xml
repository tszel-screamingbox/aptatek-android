<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/graph.bubble.radius.small" />

            <solid android:color="@color/pkuPdfFasting" />
            <size
                android:width="@dimen/pdf.legend.bubble.size"
                android:height="@dimen/pdf.legend.bubble.size" />

            <stroke
                android:width="@dimen/graph.bubble.stroke.small"
                android:color="@color/pkuPdfSick" />

        </shape>
    </item>
    <item>
        <shape android:shape="rectangle">
            <corners android:radius="@dimen/graph.bubble.radius.small" />
            <solid android:color="@color/pkuPdfFasting" />
            <size
                android:width="@dimen/pdf.legend.bubble.size"
                android:height="@dimen/pdf.legend.bubble.size" />
            <stroke
                android:width="4dp"
                android:color="@color/transparent" />
        </shape>
    </item>
</layer-list>